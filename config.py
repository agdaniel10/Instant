MIC_RATE = 16000        # Sampling rate
CHUNK_SIZE = 2048       # Frames per buffer
MODEL_SIZE = "small"    # Whisper model size ("tiny" or "small")
LANGUAGE = "en"         # Default language
BUFFER_SECONDS = 2      # Keep last 2 seconds for regex sliding window
